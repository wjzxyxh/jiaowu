# 前端和后端启动说明

## 快速启动（推荐）

### 方式一：一键启动（后台运行）

```bash
./start_all.sh
```

这个脚本会：
- 自动检查Python和Node.js环境
- 停止现有的前端和后端进程
- 在后台启动后端服务（端口5000）
- 在后台启动前端服务（端口3000）
- 显示进程ID和访问地址

### 方式二：分别启动（两个终端）

**终端1 - 启动后端：**
```bash
./start_backend.sh
# 或
python3 app.py
```

**终端2 - 启动前端：**
```bash
./start_frontend.sh
# 或
cd frontend && npm run dev
```

## 访问地址

启动成功后，可以通过以下地址访问：

- **前端界面**: http://localhost:3000
- **后端API**: http://localhost:5000/api

## 查看日志

如果使用 `start_all.sh` 启动，日志文件位置：

- **后端日志**: `backend.log`
- **前端日志**: `frontend.log`

实时查看日志：
```bash
# 查看后端日志
tail -f backend.log

# 查看前端日志
tail -f frontend.log
```

## 停止服务

### 方式一：使用停止脚本
```bash
./stop_all.sh
```

### 方式二：手动停止
```bash
# 停止后端
pkill -f "python.*app.py"

# 停止前端
pkill -f "vite"
```

## 使用screen在服务器上运行（推荐）

如果需要在服务器上长期运行，建议使用screen：

```bash
# 安装screen（如果没有）
# CentOS/RHEL: yum install screen
# Ubuntu/Debian: apt-get install screen

# 启动后端screen会话
screen -S backend
python3 app.py
# 按 Ctrl+A 然后按 D 退出screen（服务继续运行）

# 启动前端screen会话
screen -S frontend
cd frontend && npm run dev
# 按 Ctrl+A 然后按 D 退出screen（服务继续运行）

# 重新连接到screen会话
screen -r backend  # 连接后端
screen -r frontend # 连接前端

# 查看所有screen会话
screen -ls
```

## 检查服务状态

```bash
# 检查后端是否运行
ps aux | grep "python.*app.py"

# 检查前端是否运行
ps aux | grep "vite"

# 检查端口占用
netstat -tlnp | grep -E "3000|5000"
# 或
ss -tlnp | grep -E "3000|5000"
```

## 常见问题

### 1. 端口被占用

如果5000或3000端口被占用，可以：

**修改后端端口：**
```bash
PORT=5001 python3 app.py
```

**修改前端端口：**
编辑 `frontend/vite.config.js`，修改 `server.port` 为其他端口

### 2. 前端依赖未安装

```bash
cd frontend
npm install
```

### 3. 后端依赖未安装

```bash
pip3 install -r requirements.txt
```

### 4. 权限问题

如果脚本无法执行，添加执行权限：
```bash
chmod +x start_all.sh
chmod +x stop_all.sh
chmod +x start_backend.sh
chmod +x start_frontend.sh
```

## 生产环境部署建议

1. **使用进程管理器**（如PM2、supervisor）
2. **配置反向代理**（如Nginx）
3. **使用HTTPS**
4. **配置防火墙规则**
5. **设置自动重启**

